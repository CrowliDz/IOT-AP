import { Departamento } from './departamento';

export class Config {
    id_config: number;
    alarma_config: number;
    min_config: number;
    grafica_config: number;
    id_usuario: number;

    constructor() {
    }
}